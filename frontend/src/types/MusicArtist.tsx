export type MusicArtist = {
    id: string;
    name: string;
    description?: string;
    firstName?: string;
    lastName?: string;
    birthDate?: string;
}

export type MusicArtistShort = {
    id: string;
    name: string;
}